@model DMS_v2.Models.TemplateViewModel

@{
    ViewBag.Title = "_NavBar";

    String path = HttpContext.Current.Request.Url.AbsolutePath;
    var usercount = Model.UserPrivileges.Where(i => i.PrivilegeId == 1 && i.RegistrationId == Model.Registration.Id).Count();
}


<ul class="nav navbar-nav navbar-left">
    @if (Model.Registration.AccountType == "Administrator")
    {
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">ADMINISTRATION &nbsp;<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
                <li><a href="@Url.Action("Index", "RegistrationRequest")">ACCOUNT REQUEST </a></li>
                <li><a href="@Url.Action("Index", "Registration")">ACCOUNT LIST </a></li>
            </ul>
        </li>
    }

</ul>

